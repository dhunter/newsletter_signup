{
  "name": "newsletter_signup",
  "version": "1.0.0",
  "description": "An app to add a user's email address to a Mailchimp list",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dylan Hunter",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.3"
  },
  "engines": {
    "node": "14.x"
  }
}
